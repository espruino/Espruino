(function(){let c=require("Storage").readJSON("setting.json",1)||{},b=require("Storage").read(c.launcher);if(!b){b=void 0;let d=require("Storage").list(/\.info$/).map(a=>{if((a=require("Storage").readJSON(a,1))&&"launch"==a.type)return a}).filter(a=>a).sort((a,e)=>a.sortorder-e.sortorder)[0];d&&(c.launcher=d.src,require("Storage").writeJSON("setting.json",c),b=require("Storage").read(d.src))}b?Bangle.uiRemove?(Bangle.setUI(),setTimeout(eval,0,b)):load(c.launcher):eval('E.showMessage("No Launcher Found");setWatch(()=>{load();}, global.BTN2||BTN, {repeat:false,edge:"falling"});');
delete c;delete b});
